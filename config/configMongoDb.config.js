import mongoose from 'mongoose'

/* function connectToMongoDB(){
            const DB_NAME = 'PWA_UTN_2025_PROYECTO_SLACK_REAL'
            const connection_string = 'mongodb://localhost:27017/' + DB_NAME
            mongoose.connect(connection_string)
        } */